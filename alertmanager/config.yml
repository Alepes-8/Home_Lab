global:
  resolve_timeout: 5m

route:
  receiver: 'placeholder'
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: 'placeholder'
    # No notification transport configured yet.
    # To add Discord later, replace this with:
    # discord_configs:
    #   - webhook_url: 'https://discord.com/api/webhooks/YOUR_WEBHOOK'