events {}

http {
    server {
        listen 80;
        server_name api.local;

        location / {
            proxy_pass http://drink_api_prod:5001;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}